# Core Stream Processing & Messaging
confluent-kafka>=2.4.0
aiokafka>=0.11.0
# faust-streaming[rocksdb]>=0.10.0  # use this on Linux / Docker (requires pkg-config + librocksdb)
faust-streaming>=0.10.0             # base install on Windows (RocksDB extra not available without pkg-config)
pydantic>=2.7.0
pydantic-settings>=2.2.0

# Backend & Web API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.9
httpx>=0.27.0
websockets>=12.0
python-dotenv>=1.0.1

# Observability & Monitoring
prometheus-client>=0.20.0

# Testing & Benchmarking
pytest>=8.1.0
pytest-asyncio>=0.23.6
psutil>=5.9.8
